import { Box, Typography } from '@mui/material';

export function DashboardPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" fontWeight={700}>
        landing page with exedocs - Dashboard
      </Typography>
    </Box>
  );
}
