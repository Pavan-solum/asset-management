import { Box, Typography } from '@mui/material';

export function FinancePage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" fontWeight={700}>
        landing page with exedocs - Finance
      </Typography>
    </Box>
  );
}
