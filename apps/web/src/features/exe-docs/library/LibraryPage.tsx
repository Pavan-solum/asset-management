import { Box, Typography } from '@mui/material';

export function LibraryPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" fontWeight={700}>
        landing page with exedocs - Library
      </Typography>
    </Box>
  );
}
