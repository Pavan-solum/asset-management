# Project Rules & Customizations

## Git Push Protocol
- **NEVER push code to remote repositories (`git push`) without explicit confirmation and permission from the user.**
- Always build, test, and commit locally, then ask the user before running any push commands.
